public class Page
{
    @foreach (var element in @Model.WebElementDefinitions ) 
    {
        <text>
            [FindsBy(How=How.@Raw(@element.How), Using = @@"@Raw(@element.QuotedLoacator)")]
            public IWebElement @Raw(@element.Name) { get; set; }
        </text>
    }
}